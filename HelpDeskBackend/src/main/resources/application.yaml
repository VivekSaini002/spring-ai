spring:
  application:
    name: HelpDeskBackend
  #datasource configurations
  datasource:
    url: jdbc:mysql://localhost:3307/spring_ai?useSSL=false&serverTimezone=UTC
    username: root
    password: viveksaini9758
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-2.5-flash-lite

    chat:
      memory:
        repository:
          jdbc:
            initialize-schema: always
#  profiles:
#    active: dev

logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG


#another:
#  student: true